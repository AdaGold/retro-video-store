# from flask import current_app
# from app import db

# class CustomerVideoJoin(db.Model):
#     id = db.Column(db.Integer, primary_key=True, autoincrement=True)
#     customer_id = db.Column(db.Integer, db.ForiegnKey("customer.id"), primary_key=True)
#     video_id = db.Column(db.Integer, db.ForiegnKey("video.id"), primary_key=True)